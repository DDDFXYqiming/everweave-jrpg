# Everweave · 未写之境

写下一段世界设定，走进由语言模型持续创作的像素 RPG。

新世界先由模型规划一段有目标的章节，再逐个生成其中的地区。你可以探索分支路线、调查跨地区线索、发现隐藏通道；启用战斗或物品系统时，也可以战斗与收集物品。走路、碰撞和已有规则在本地执行，冒险进度自动保存。

Everweave 使用 Godot 显示游戏，Python 管理世界状态。模型在开局选择系统、角色称谓和资源，画面与声音在适合题材时复用本地素材，缺少合适素材则原创。目前仍是开发原型，生成速度、地图密度和谜题质量会随模型与题材变化。章节系统的完整实机试玩尚待验证，详见 [本轮交付记录](docs/CAMPAIGN_VALIDATION.md)。

## 游戏画面

下面三组场景来自章节系统加入前的 `a02ad4e` 阶段，由实际 Godot 客户端渲染。它们展示美术效果，不作为当前章节、隐藏路线或跨地区玩法的验收证据。点击图片可以查看大图。

![潮雾港的修船村](docs/showcase/harbor.png)

| 地下天文馆 | 轨道气象站 |
|---|---|
| ![地下天文馆](docs/showcase/ruins.png) | ![轨道气象站](docs/showcase/orbital.png) |

场景设定、生成方式和截图记录见 [示例世界](docs/SHOWCASE.md)。

## 开始一段旅程

需要 Python 3.11 或更高版本，以及 Godot 4 Standard。项目当前使用 Godot 4.7.2 测试。

Windows 用户可以把 Godot 可执行文件放在项目根目录或 `tools/`，然后双击 `Start.cmd`。也可以从命令行启动。

```powershell
python launch.py --godot ./tools/Godot.exe
```

如果 Godot 已加入 PATH，运行 `python launch.py` 即可。首次启动会下载清单中缺少的素材并核对文件哈希；需要重建拼接图时，相关工具安装在项目内的缓存目录。准备好资源以后，启动器会打开游戏并运行本机服务。

在游戏中填写世界设定，配置模型服务，然后开始新旅程。默认支持 DeepSeek，也可以填写兼容 Chat Completions 与 JSON 输出的服务。API Key 在设置中输入，或通过 `DEEPSEEK_API_KEY` 环境变量提供。

开始菜单上方可以切换简体中文和 English，游戏中也能从设置进入。界面立即更新，后续生成使用所选语言；已经保存的任务、剧情和对白保留原文。语言选择会保存，详细说明见 [语言设置](docs/LOCALIZATION.md)。

可以从这样一段设定开始。

> 我是来到潮雾港的测潮员。修船村的绞盘坏了，一艘小船被困在木码头。我想向船匠打听故障，找到灯塔值班室留下的维修记录。

在线生成会产生模型服务费用。界面可以设置调用上限、查看用量，也可以暂停后续生成。初次创建世界和准备新区域需要等待；已经生成的地图仍可继续探索。只想检查安装和操作时，可以选择不调用模型的离线自检。

## 操作

| 操作 | 按键 |
|---|---|
| 移动 | WASD 或方向键 |
| 与相邻人物、物体或出口交互 | E 或空格 |
| 查看当前自定义操作 | F |
| 物品与记录面板（称谓随世界变化） | I、J；物品系统关闭时 I 不打开面板 |
| 展开旅途地图 | G，或点击小地图 |
| 等待 | `.` |
| 选择战斗操作 | 数字键，具体动作以界面为准 |
| 关闭面板 | Esc |
| 全屏 | F11 |
| 音乐开关 | M |

一个存档目录保存一个世界。想保留当前旅程并另开一局，可以指定独立目录。

```powershell
python launch.py --data-dir ./userdata/another-journey
```

安装、存档位置与模型设置的详细说明见 [使用指南](docs/LOCAL_SETUP.md)。

## 世界怎样生成

在线新世界先生成游戏规格和章节计划：本章目标、4～8 个地点、分支与回环、共享线索和完成条件。随后模型根据每个地点的职责与一小组适用素材生成蓝图及玩法数据。后台沿连接图提前准备附近地区，重要事件也可以引起局部变化。

地区之间通过章节共享状态关联，例如电站恢复供电后另一地区的门禁解除。章节完成后再规划下一章，通过预留出口接续，已生成地区仍可返回。界面显示章节目标及进展，物品面板、角色称谓和资源按游戏规格呈现。战斗、物品、装备和成长可以分别选择启用；关闭的系统在规则校验和操作执行中也受到限制。

地图尺寸、房间位置和对象行为由生成内容决定。草地、道路和墙面的细节由本地材质配方补充；装饰按固定种子布置，避免要求模型逐格画图。音乐可以随地区、战斗或剧情切换。

目前素材库主要覆盖小镇与地下主题。现代医院、感染事件和科幻设定不会默认套用中世纪角色；没有相容候选时需要原创。检索仍基于关键词和标签，不是完整的语义理解。结构校验能发现越界、引用和执行问题，无法保证整章谜题可解、剧情连贯或长期游玩稳定。当前能力与限制见 [章节与游戏规格](docs/CAMPAIGNS.md)。

## 文档与开发

[文档目录](docs/README.md) 包含架构、内容协议和素材扩展说明。历史实验记录与当前使用指南分开列出。

```powershell
python -m unittest discover -s tests -v
python -m engine.asset_cache --verify
```

Godot 原生测试的准备方法见 [开发与测试](docs/DEVELOPMENT.md)。运行时密钥、个人存档和生成日志均不应提交到仓库；提交问题时请先检查附件是否包含私人内容。

## 许可

项目自有代码和文档采用 [MIT License](LICENSE)。第三方素材保留各自许可证，来源及作者见 [素材署名](assets/library/CREDITS.md)。完整说明见 [授权说明](LICENSE-NOTICE.md)。
