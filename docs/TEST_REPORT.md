# 本次交付的验证记录

日期：2026-09-11。此报告区分实际执行、静态检查和未执行的部分。

## 实际执行并通过

命令：

```text
python -m unittest discover -s tests -v
```

结果：**70 项测试全部通过，最后一轮耗时约 5.7 秒。** 原始条目见 [test-results.txt](test-results.txt)。

覆盖的主要合同：

| 范围 | 实际验证 |
|---|---|
| 生成地图 | 6 种布局 × 7 种生态 × 6 个种子，共 252 个组合；实体、出口、返回落点均可达；相同种子重现相同地图 |
| 游戏过程 | 真正逐格移动到 NPC；选择后新地区变化；开箱与任务不重复领奖；商店扣款；装备；药剂；完整战斗胜利；禁魔规则 |
| 世界持续性 | 访问地图不可重写；进入新区域后能回原图；访问、库存、战斗经过 SQLite 保存/重载保持；待处理世界反应跨重启保留 |
| 异步 / 费用 | 一个调度器；最多两个相邻预取；步行不调用模型；调用总量限制；错误 JSON 最多修复一次；网络失败不自动重试；过期世界/配置/事件结果丢弃；交互中延迟应用反应 |
| 真实本机 HTTP | Chat Completions 模拟服务、JSON mode 和非思考字段、usage 记账、截断拒绝、重定向拒绝；本机游戏 API 认证、Origin 拒绝、请求体限制、购买去重 |
| 安全与资产 | 模型不可修改 HP/金币；错误补丁回滚；坏任务引用拒绝；道具数值限制；接口 Key 不在快照/SQLite 中；PNG 图集坐标有效、PCM 音频有效、资源引用存在、未打包字体 |

这些测试没有消耗真实大模型额度。HTTP 模拟服务在本机回环地址运行，返回符合协议的样例 JSON；它只证明协议和状态链路，不证明某个真实模型的内容质量。

还实际执行了 `python -m compileall`（Python 语法检查）、`python tools/publish_private.py --dry-run`（上传文件清单检查），并检查了生成的像素图集与离线地图数据预览。

## 未完成的原生 / 线上验证

**Godot 原生客户端没有在本制作环境启动。** 环境没有 Godot 可执行文件，官方二进制下载未能成功。因此不能把 Python 核心测试或图片预览当作 GDScript 原生编译、UI 排版、键盘操作、音频播放、帧率或导出包已经验证。

已提供 `tests/client_smoke.gd` 与 GitHub Actions 检查配置，用真实 Godot 验证主页、地图、背包、战斗和对话场景的实例化；**本次没有执行它们，也没有云端 CI 成功记录**。

**没有真实 DeepSeek 请求。** 未提供模型 Key，未实测真实生成延迟、质量、成本或服务兼容性。接口适配依据官方文档，且通过本机模拟接口验证。首次使用应保留较小调用上限，观察右侧错误和 usage。

**没有创建或上传 GitHub 仓库。** 当前连接器缺少建库动作。发布脚本已附，但没有在拥有者 GitHub 账号上执行，因此没有真实仓库地址、远端提交 SHA 或上传成功记录。

## 原型边界

这是源代码原型，而不是声称已经完成全平台发行验收的游戏。素材数量、战斗/任务原语、单角色和单存档槽均有意保持有限。后台与界面有明确测试入口，便于在目标 Windows + Godot 环境继续验证，而不是把未运行部分写成“已完成测试”。
